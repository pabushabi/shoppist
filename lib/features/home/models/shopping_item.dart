class ShoppingItemModel {
  String name;
  int amount;
  int maxAmount;
  String type;

  ShoppingItemModel({
    required this.name,
    required this.amount,
    required this.maxAmount,
    required this.type,
  });
}
